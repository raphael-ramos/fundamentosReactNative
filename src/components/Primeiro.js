import React from 'react';
import {Text} from 'react-native'

import Estilo from './estilo'

export default () => {
    // console.warn('Opa!')  //exibe alerta na tela, pode ser usado no debug da mesma forma que o console.log()
    return (
        <Text style={Estilo.txtG}>a</Text>
    )
}